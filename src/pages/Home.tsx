import { motion, AnimatePresence } from "framer-motion";
import styled from "styled-components";
import { useRef, useEffect, useState } from "react";

function Home() {
  return (
    <div style={{minHeight: '200vh'}}>homt</div>
  );
}

export default Home;